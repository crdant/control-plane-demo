#!/usr/bin/env bash
# Pre-commit hook for Helm/Replicated project

set -e

echo "🔧 Running pre-commit checks..."

# Skip formatting for Helm/Replicated projects as there are no standard formatters
echo "📝 Skipping code formatting (not applicable for Helm charts)"

echo "🚀 Pre-commit checks passed!"